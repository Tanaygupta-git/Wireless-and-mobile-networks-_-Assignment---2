function parseURL(url) {
    let parser = new URL(url);

    return {
        protocol: parser.protocol,
        host: parser.host,
        pathname: parser.pathname
    };
}

console.log(parseURL("https://example.com/test/page"));
